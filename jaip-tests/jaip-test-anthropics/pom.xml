<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.igormaznitsa</groupId>
        <artifactId>jaip-tests</artifactId>
        <version>0-SNAPSHOT</version>
    </parent>

    <artifactId>jaip-test-anthropics</artifactId>
    <packaging>jar</packaging>

    <properties>
        <jaip.prompt.cache.file>${project.basedir}/jaip_cache.json</jaip.prompt.cache.file>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>properties-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>load-secret-properties</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>read-project-properties</goal>
                        </goals>
                        <configuration>
                            <files>
                                <file>${project.basedir}/secret_properties.properties</file>
                            </files>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.igormaznitsa</groupId>
                <artifactId>jcp</artifactId>
                <executions>
                    <execution>
                        <id>preprocessSources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>preprocess</goal>
                        </goals>
                        <configuration>
                            <keepLines>false</keepLines>
                            <allowBlocks>true</allowBlocks>
                            <preserveIndents>true</preserveIndents>
                            <vars>
                                <jaip.anthropic.model>${jaip.anthropic.model}</jaip.anthropic.model>
                                <jaip.anthropic.api.key>${jaip.anthropic.api.key}</jaip.anthropic.api.key>
                                <jaip.prompt.cache.file>${jaip.prompt.cache.file}</jaip.prompt.cache.file>
                            </vars>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>com.anthropic</groupId>
                        <artifactId>anthropic-java</artifactId>
                        <version>2.1.0</version>
                    </dependency>
                    <dependency>
                        <groupId>com.igormaznitsa</groupId>
                        <artifactId>jaip-anthropic</artifactId>
                        <version>${jaip.test.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>
